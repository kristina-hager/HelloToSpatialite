<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    tools:context=".MainActivity">

    <TextView
        android:id="@+id/communicate"
        android:text="@string/hello_world"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content" />

    <Button
        android:id="@+id/get_versions"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/communicate"
        android:onClick="getVersionInfo"
        android:text="@string/get_version_button"
        />

    <Button
        android:id="@+id/run_tests"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/get_versions"
        android:onClick="runSimpleTest"
        android:text="@string/run_simple_test_button"
        />

    <Button
        android:id="@+id/run_point_in_polygon"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/run_tests"
        android:onClick="runPointInPolygon"
        android:text="@string/run_point_in_polygon"
        />

</RelativeLayout>
